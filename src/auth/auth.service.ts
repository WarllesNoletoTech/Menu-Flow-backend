import { ConflictException, Injectable, UnauthorizedException } from '@nestjs/common'; import { JwtService } from '@nestjs/jwt'; import { InjectModel } from '@nestjs/mongoose'; import * as bcrypt from 'bcrypt'; import { Model } from 'mongoose'; import { Role } from '../common/roles'; import { User, UserDocument } from '../common/schemas';
@Injectable() export class AuthService { constructor(@InjectModel(User.name) private readonly users: Model<UserDocument>, private readonly jwt: JwtService) {} async bootstrap(name: string, email: string, password: string) { if (await this.users.exists({})) throw new ConflictException('Bootstrap is only available before the first user'); return this.create(name, email, password, Role.SUPER_ADMIN); } async create(name: string, email: string, password: string, role: Role, restaurantId?: string) { if (await this.users.exists({ email })) throw new ConflictException('Email already exists'); const user = await this.users.create({ name, email, passwordHash: await bcrypt.hash(password, 12), role, restaurantId }); return { id: user.id, email: user.email, role: user.role }; } async login(email: string, password: string) { const user = await this.users.findOne({ email }).select('+passwordHash'); if (!user || !user.active || !(await bcrypt.compare(password, user.passwordHash))) throw new UnauthorizedException('Invalid credentials'); return { accessToken: await this.jwt.signAsync({ sub: user.id, role: user.role, restaurantId: user.restaurantId?.toString() }), user: { id: user.id, name: user.name, role: user.role, restaurantId: user.restaurantId } }; } }
