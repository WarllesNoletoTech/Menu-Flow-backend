import { Module } from '@nestjs/common'; import { MongooseModule } from '@nestjs/mongoose'; import { AuthModule } from '../auth/auth.module'; import { Category, CategorySchema, Product, ProductSchema } from '../common/schemas'; import { AdminCatalogController, CatalogController, MerchantCatalogController } from './catalog.controller'; import { CatalogService } from './catalog.service';
@Module({ imports: [AuthModule, MongooseModule.forFeature([{ name: Category.name, schema: CategorySchema }, { name: Product.name, schema: ProductSchema }])], controllers: [CatalogController, MerchantCatalogController, AdminCatalogController], providers: [CatalogService] }) export class CatalogModule {}
