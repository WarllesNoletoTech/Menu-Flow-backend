const test=require('node:test');const assert=require('node:assert/strict');const {firstTuesdayOfMonth,isFirstTuesday,MENU_FLOW_ORDER_SERVICE_FEE_CENTS}=require('../dist/billing/billing-rules');const {renderBillingReportPdf}=require('../dist/billing/billing-pdf');
test('taxa é R$ 1 e primeira terça é calculada no timezone',()=>{assert.equal(MENU_FLOW_ORDER_SERVICE_FEE_CENTS,100);assert.equal(firstTuesdayOfMonth(2026,9,'America/Sao_Paulo'),1);assert.equal(firstTuesdayOfMonth(2026,10,'America/Sao_Paulo'),6);assert.equal(isFirstTuesday(new Date('2026-09-01T15:00:00Z'),'America/Sao_Paulo'),true);assert.equal(isFirstTuesday(new Date('2026-09-08T15:00:00Z'),'America/Sao_Paulo'),false)});
test('PDF real pagina 200 pedidos, contém marca vetorial, resumo e páginas',()=>{const report={reportNumber:'MF-RS-2026-000123',periodStart:'2026-09-02T03:00:00Z',periodEnd:'2026-09-09T02:59:59Z',generatedAt:'2026-09-09T12:00:00Z',orderCount:200,serviceFeePerOrderCents:100,serviceFeeTotalCents:20000,includeMonthlyFee:true,monthlyFeeCents:15000,totalCents:35000,timezone:'America/Sao_Paulo',restaurantSnapshot:{name:'Sabor da Praça',cnpj:'00.000.000/0001-00',city:'Redenção',state:'PA'}};const items=Array.from({length:200},(_,i)=>({orderNumber:`MF-${i+1}`,completedAt:'2026-09-03T15:35:00Z',fulfillment:i%2?'DELIVERY':'PICKUP',orderTotalCents:4900,feeCents:100}));const pdf=renderBillingReportPdf(report,items);assert.equal(pdf.subarray(0,8).toString(),'%PDF-1.4');assert.match(pdf.toString('latin1'),/Menu Flow/);assert.match(pdf.toString('latin1'),/MF-RS-2026-000123/);assert.match(pdf.toString('latin1'),/TOTAL A PAGAR/);assert.match(pdf.toString('latin1'),/\/Count 7/)});
